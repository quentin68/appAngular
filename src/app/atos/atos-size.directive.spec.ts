import { AtosSizeDirective } from './atos-size.directive';

describe('AtosSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AtosSizeDirective();
    expect(directive).toBeTruthy();
  });
});
