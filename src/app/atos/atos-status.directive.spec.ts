import { AtosStatusDirective } from './atos-status.directive';

describe('AtosStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new AtosStatusDirective();
    expect(directive).toBeTruthy();
  });
});
